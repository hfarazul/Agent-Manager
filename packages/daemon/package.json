{
  "name": "@agent-hud/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Agent HUD daemon — single source of truth for Claude Code session/sleep/usage state",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "agent-hud-daemon": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "AGENT_HUD_READY_TO_IDLE_MS=50 node --import tsx --test src/hooks.test.ts src/store.test.ts src/attention.test.ts src/usage.test.ts src/project.test.ts src/notifier.test.ts src/server.test.ts"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
